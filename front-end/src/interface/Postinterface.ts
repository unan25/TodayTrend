export type PostType = {
  postId?: string;
  imageId?: string;
  imageUrl?: string;
  id?: string;
  url?: string;
};

export type ImageType = {
  imageId?: string;
  imageUrl?: string;
  postId?: string;
};
